@startuml seq-test-connection
' Robustness / Boundary-Control-Entity (BCE) sequence diagram.
' Visual skin kept black-and-white / Times New Roman to match the report
' (enforced by _build/restyle_seq.py); the structure follows the BCE rules.
skinparam backgroundColor transparent
skinparam shadowing false
skinparam maxMessageSize 200
skinparam defaultFontName Times New Roman
skinparam sequence {
  ArrowColor #000000
  ArrowFontColor #000000
  LifeLineBorderColor #333333
  LifeLineBackgroundColor #FFFFFF
  ParticipantBackgroundColor #FFFFFF
  ParticipantFontColor #000000
  ParticipantBorderColor #333333
  ActorBackgroundColor #FFFFFF
  ActorBorderColor #333333
  DatabaseBackgroundColor #FFFFFF
  DatabaseBorderColor #333333
}
hide footbox
autonumber
title Settings connection test

actor ":User" as User
boundary ":InterfaceSettings" as UI
control ":ControllerLiveProxy" as Proxy
control ":WorkflowN8N" as N8N

User -> UI : testConnection(target)
UI -> Proxy : checkTarget(target)
Proxy -> Proxy : validateEndpoint(target)
Proxy -> N8N : forwardPing(pingRequest)
alt workflow reachable
  N8N --> Proxy : pong(ok)
  Proxy --> UI : connectionSucceeded(status)
else unreachable or timeout
  Proxy -> Proxy : handleTimeout()
  Proxy --> UI : connectionFailed(reason)
end
@enduml
